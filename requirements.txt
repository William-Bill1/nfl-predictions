# NFL Predictions - Streamlit Application Requirements
#
# Pinned to an end-to-end validated set (app boot, smoke_test.py, pytest,
# player-prop model load all pass). See requirements-dev.txt for test/dev extras.
#
# Python: 3.12 or 3.13.  NOT 3.11 - the app uses PEP 701 f-strings
#         (backslashes inside f-string expressions) which are a SyntaxError
#         before 3.12.
#
# To refresh these pins: create a clean venv, `pip install` the unpinned
# top-level names below, verify with `python smoke_test.py` and `pytest -q`,
# then `pip freeze` and copy the resulting versions here.
#
# NOTE on nfl-data-py: keep it pinned to ==0.3.2. Version 0.3.3 hard-pins
# `numpy<2` / `pandas<2`, which have no wheels for modern Python and force a
# (failing) source build. 0.3.2 only needs `pandas>1` and installs cleanly.

# --- Core app + model training ---
streamlit==1.62.0
pandas==2.3.3
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
requests==2.34.2
python-dotenv==1.2.3
reportlab==5.0.1
plotly==7.0.0

# --- Data ingestion (create-nfl-historical.py, update_schedule.py, backtests) ---
nfl-data-py==0.3.2

# --- Player props: ESPN injury scraping (player_props/injuries.py) ---
beautifulsoup4==4.14.2

# --- Player props: weather adjustments (player_props/weather.py) ---
openmeteo-requests==1.7.5
requests-cache==1.3.3
retry-requests==2.0.0

# Production extras (uncomment if deploying as a plain web server)
# gunicorn>=21.2.0
# watchdog>=3.0.0
